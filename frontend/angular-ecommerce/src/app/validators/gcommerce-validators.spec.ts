import { GCommerceValidators } from './gcommerce-validators';

describe('GCommerceValidators', () => {
  it('should create an instance', () => {
    expect(new GCommerceValidators()).toBeTruthy();
  });
});
